// url a conexion DB desde controller
const baseUrlconn = '../../../config/conexion';
// url a la carpeta persisntence desde controller 
const baseUrlPers = '../../persistence/';

module.exports = {
    baseUrlconn,
    baseUrlPers 

}